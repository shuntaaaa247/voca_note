export const UI_DATA = {
  createItemModal: { // アイテム作成モーダル
    screenClassName: "fixed top-0 left-0 w-full h-full bg-slate-100 bg-opacity-80 z-10",
    modalClassName: "fixed top-[20%] left-[20%] w-[60%] h-[60%] bg-white rounded-xl shadow-xl z-20"
  },
  createCategoryModal: { // カテゴリ作成モーダル
    screenClassName: "fixed top-0 left-0 w-full h-full bg-slate-100 bg-opacity-80 z-10",
    modalClassName: "fixed top-[20%] left-[20%] w-[60%] h-[60%] bg-white rounded-xl shadow-xl z-20"
  },
  selectionModalWidth: 120, // アイテムの[削除、編集]選択モーダルの横幅
  selectionModal: {
    screenClassName: "fixed top-0 left-0 w-full h-full z-10",
    modalClassName: "fixed w-[120px] h-1/12 bg-white rounded-xl shadow-xl z-20"
  },
  confirmDeleteModal: {
    screenClassName: "fixed top-0 left-0 w-full h-full z-10",
    modalClassName: "fixed h-1/12 bg-white rounded-xl shadow-xl z-20"
  }
}
